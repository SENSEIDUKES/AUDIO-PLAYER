--- src/audio-player/visual-slots/components/imported/sample-skin/SampleSkin.tsx
+++ src/audio-player/visual-slots/components/imported/sample-skin/SampleSkin.tsx
@@ -16,10 +16,14 @@

 /** Default settings for this skin. Edit to match your component's API. */
-export const sampleSkinDefaultSettings: Record<string, unknown> = {
-    // TODO(skin): add default settings that your component needs.
-}
+export const sampleSkinDefaultSettings: SampleSkinSettings = {
+    primaryColor: "#7cc4ff",
+    label: "Hello Skin"
+};

-type SampleSkinSettings = Record<string, unknown>
+type SampleSkinSettings = {
+    primaryColor?: string;
+    label?: string;
+}

 // ─── Component ───────────────────────────────────────────────────────────────

@@ -28,8 +32,7 @@
-    void settings
     void playback
     return (
         <div className="sap-visual-sample-skin">
-            <RawComponent />
+            <RawComponent primaryColor={settings.primaryColor} label={settings.label} />
         </div>
     )
 }
