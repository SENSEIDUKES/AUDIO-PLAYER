# Historical package setup note

This file records the initial library-packaging pass. It is retained for
repository history only and is not a publishing or consumer-install guide.

For the current package contract, use:

- [`README.md`](./README.md) for installation, a working React example, and the
  local Lab.
- [`PUBLISHING_GUIDE.md`](./PUBLISHING_GUIDE.md) for npm distribution,
  unreleased Git/local-link workflows, package contents, and release-owner steps.
- [`docs/public-api.md`](./docs/public-api.md) for the public API families and
  supported import boundaries.

The package's current metadata and scripts in `package.json` are authoritative.
